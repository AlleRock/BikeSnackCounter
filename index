<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Bike Stats</title>
    <link rel="apple-touch-icon" href="icon.png">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <style>
        body { font-family: sans-serif; background: #121212; color: white; text-align: center; margin: 0; padding: 20px; }
        .display { background: #2ecc71; padding: 20px; border-radius: 20px; margin-bottom: 20px; }
        .val { font-size: 3rem; font-weight: bold; }
        .row { background: #2a2a2a; padding: 15px; border-radius: 15px; margin-bottom: 10px; display: flex; justify-content: space-between; align-items: center; }
        .btn { border: none; padding: 15px 25px; border-radius: 10px; color: white; font-weight: bold; font-size: 1.2rem; }
        .btn-plus { background: #3498db; }
        .btn-minus { background: #e74c3c; }
        input { background: #000; color: white; border: 1px solid #444; padding: 10px; width: 60px; border-radius: 10px; }
        .export { background: #f1c40f; color: black; width: 100%; padding: 20px; margin-top: 20px; border-radius: 15px; font-weight: bold; }
    </style>
</head>
<body>

    <div id="cibo">
        <div class="display">
            <div>Kcal Cibo</div>
            <div class="val" id="k">0</div>
        </div>
        <div class="row"><span>FOCACCIA</span><button class="btn btn-plus" onclick="u('f',1,320)">+</button></div>
        <div class="row"><span>PAPPONE</span><button class="btn btn-plus" onclick="u('p',1,119)">+</button></div>
        <div class="row"><span>SMURFI</span><button class="btn btn-plus" onclick="u('s',1,135)">+</button></div>
        <div class="row"><span>BIFI</span><button class="btn btn-plus" onclick="u('b',1,108)">+</button></div>
    </div>

    <hr>

    <div id="acqua">
        <div class="display" style="background:#3498db">
            <div>Acqua (cl)</div>
            <div class="val" id="w">0</div>
        </div>
        <div class="row">
            <input type="number" id="wi" placeholder="cl">
            <button class="btn" style="background:#2ecc71" onclick="addW()">OK</button>
        </div>
    </div>

    <button class="export" onclick="ex()">🚀 ESPORTA MAIL</button>
    <button onclick="location.reload()" style="background:none; border:none; color:grey; margin-top:20px">Azzera Pagina</button>

<script>
    let tk = 0, tw = 0;
    function u(id, d, v) { tk += v; document.getElementById('k').innerText = tk; }
    function addW() { 
        let v = parseInt(document.getElementById('wi').value);
        if(v) { tw += v; document.getElementById('w').innerText = tw; document.getElementById('wi').value=''; }
    }
    function ex() {
        let b = `Kcal: ${tk}%0DAcqua: ${tw}cl`;
        window.location.href = `mailto:m8online@gmail.com?subject=Giro Bici&body=${b}`;
    }
</script>
</body>
</html>
